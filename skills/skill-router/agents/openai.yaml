interface:
  display_name: "Skill Router"
  short_description: "Select and apply relevant skills"
  default_prompt: "Use $skill-router to choose the most relevant existing skills before solving the task."

policy:
  allow_implicit_invocation: true
